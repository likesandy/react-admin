import styled from 'styled-components'

export const MarkDownWrapper = styled.div`
  display: flex;
  flex-direction: column;
  .submit {
    display: inline;
    text-align: right;
    overflow: hidden;
  }
`
